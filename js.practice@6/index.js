let Age = Number(prompt("enter your age"));
console.log(Age);

if (Age === 30) {         // if like a first check     === when user age greetthan > 30 == means they is old ;
  console.log("you are old");
} else if (Age >= 18) {         // else if is like a second check ==== when user age less && equals to 18 (>=),so else if `you are a adult ; 
  console.log("you are adult");
} else                       // else is a seconde sub checker === when user age not (>`greet than`&& = `equals than 18) else prints you are kid ; 